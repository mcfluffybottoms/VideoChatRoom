services:
  web:
    build: .
    working_dir: /app/server
    ports:
      - "${HOST_PORT:-3000}:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
    restart: unless-stopped
    # Keep exactly one replica: room/chat state is held in process memory.
